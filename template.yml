version: '1'
hosts:
  local-docker:
    description: 'The local docker host'
    host: docker.local
    user: 'vagrant'
    private_key: |
      -----BEGIN PRIVATE KEY-----
      ...
      -----END PRIVATE KEY-----
targets:
  sshd-service-log:
    host: local-docker
    file: '/var/log/auth.log'
