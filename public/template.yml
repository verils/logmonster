version: '1'
servers:
  - name: 'dev-infra'
    description: 'The CloudOS infra host of develop enviroment'
    group: 'dev'
    host: 10.10.13.2
    authentication:
      type: 'password'
      user: 'root'
      credential: 'password'
    targets:
      - name: 'sshd-service-log'
        file: '/var/log/auth.log'
      - name: 'factory-service-log'
        docker_container: 'factory'
        